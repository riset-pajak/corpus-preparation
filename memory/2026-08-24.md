# 2026-08-24

## Activities
- Audit seluruh dokumentasi (README.md, TASKS.md, SCHEMA.md, MEMORY.md, .github/copilot-instructions.md) terhadap kondisi kode aktual
- Verifikasi langsung: jumlah test (`pytest`: 31 passed + 1 skipped), isi `data.db` (110/1.360/161), perilaku `extract_identifier()` pada berbagai format, schema SQLite aktif, dan daftar command CLI
- README: tambah tabel command CLI lengkap (termasuk `add-pajak-gov-id` yang sebelumnya tidak terdokumentasi), catat venv Windows (`venv/Scripts/activate`), perbaiki tabel identifier web, catat fallback naming ingest web, koreksi struktur folder (enrichers/exporters/processors kosong)
- SCHEMA.md: hapus kesan FTS5 sudah implemented -- pindahkan DDL FTS5 ke blok "desain yang akan dipakai" dengan catatan status belum diimplementasikan; tambah index `idx_sections_regulation` yang sebenarnya ada; perbaiki tabel identifier + daftar keterbatasan parser
- TASKS.md: sinkron test count dan status collector; tambah catatan status dokumentasi
- copilot-instructions.md: tulis ulang -- tambahkan database.py, collectors/, command crawl & add-pajak-gov-id, status FTS5, keterbatasan identifier, path venv Windows, plan tersinkron TASKS.md
- MEMORY.md: catat bahwa schema aktif tanpa FTS5 dan configs/sources.yaml di-gitignore

## Decisions
- Dokumentasi harus membedakan "implemented" vs "direncanakan": contoh DDL FTS5 tetap disimpan sebagai desain, bukan bagian schema aktif
- Klaim angka (jumlah test, isi DB) hanya ditulis setelah diverifikasi lewat eksekusi nyata
- Keterbatasan parser identifier (nomor non-numerik seperti `488/KMK.010/2026`) didokumentasikan eksplisit dan ditautkan ke Langkah 1 audit

## Verification
- `pytest tests/ -q` -> 31 passed, 1 skipped (reportlab tidak terpasang)
- Query `data.db` -> regulations=110, sections=1360, topics=161; tabel aktif hanya regulations/sections/topics (tanpa FTS)
- `riset-pajak --help` -> 8 command terverifikasi
- `extract_identifier()` diuji pada 6 format input; hasil dicatat di README/SCHEMA yang revisi
